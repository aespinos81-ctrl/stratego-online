// Por ahora la aplicación es solo el juego.
// Cuando conectemos con el servidor, aquí es donde irá el menú (crear sala /
// unirse con código / buscar partida) y el juego pasará a ser una pantalla más.
import Stratego from "./Stratego.jsx";

export default function App() {
  return <Stratego />;
}
