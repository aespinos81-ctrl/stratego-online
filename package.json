{
  "name": "stratego-online",
  "version": "0.1.0",
  "private": true,
  "description": "Stratego multijugador: reglas compartidas + servidor autoritativo + cliente",
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js"
  },
  "devDependencies": {
    "socket.io-client": "^4.8.3"
  }
}
